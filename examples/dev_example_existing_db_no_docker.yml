version: 1.0
# for local execution without docker container and PostgresDB on localhost
database:
  host: localhost
  port: 5432              # port of existing PostgreSQL server on localhost
  dbname: postgres        # target database
  username: postgres      # local postgres user
  password: postgres
  on_existing: abort # skip, delete, abort
import:
  type: gip
  filename_A: A_routingexport_ogd_split.zip
